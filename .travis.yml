language: python
python:
  - "2.7"
  - "3.6"

install:
  - pip install cxroots
  - pip download cxroots --no-deps
  - tar -xzf cxroots-*.tar.gz
  - cd cxroots-*

script:
  - travis_wait python setup.py test --addopts '-v -n 2'
